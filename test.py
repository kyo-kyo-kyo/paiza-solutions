def changeName(student_list,student_number_to_update,new_student_name):
    actual_index = student_namber_to_update - 1
    if 0 <= actual_index < len(student_list):
        student_list[actual_index][0] = new_student_name
    #else:
    
    
nk_line = input().split()
N = int(nk_line[0])
K = int(nk_line[1])

all_student_info = []
for _ in range(N)
    info_line = input().split()
    all_student_info.append(info_line)
    
for _ in range(K)
    update_line = input().split()
    new_name = update_line[1]
    
    student_number_int = int(student_number_str)
    





#info = [input().split()for _ in range(int(N[0]))]
#change = [input().split()for _ in range(int(N[1]))]


#for k_idx in range (int(N[1])):
#    if info[k_idx] == int(change[k_idx][0])-1:
#        info[k_idx][0] = change[k_idx][1]
#    
#        int(change[k_idx][0])
#        change[k_idx][1]